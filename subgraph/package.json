{
  "name": "moondao-marketplace-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ moondao-marketplace-test",
    "create-local": "graph create --node http://localhost:8020/ moondao-marketplace-test",
    "remove-local": "graph remove --node http://localhost:8020/ moondao-marketplace-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 moondao-marketplace-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
